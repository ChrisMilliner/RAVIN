# RAVIN local environment configuration
# Copy this file to .env before adding local configuration.
# Never place secrets in this example file.

APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8000

# Future local AI configuration
LLM_MODEL=
EMBEDDING_MODEL=